
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    root: {
        position: 'fixed',
        bottom: 0,
        paddingBottom: 10,
        paddingLeft: '95%',
    },
}));